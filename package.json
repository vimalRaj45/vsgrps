{
  "name": "vsgrps_agile",
  "version": "1.0.0",
  "description": "VSGRPS Agile Workspace Secure Root",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
